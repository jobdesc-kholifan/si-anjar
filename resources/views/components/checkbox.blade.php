<label for="{{ $id }}">
    <input type="checkbox" id="{{ $id }}" name="{{ $name }}" value="{{ $value }}">
    <span>{{ $label }}</span>
    <span id="fullName" class="d-none">{{ $fullName }}</span>
</label>
